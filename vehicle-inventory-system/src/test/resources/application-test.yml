spring:
  datasource:
    url: jdbc:h2:mem:vehicleinventory-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

app:
  jwt:
    secret: test-only-secret-key-not-for-production-use-32bytes
  admin:
    seed-password: ""

supabase:
  url: https://example.supabase.co
  service-role-key: test-key
  storage-bucket: vehicle-photos-test
